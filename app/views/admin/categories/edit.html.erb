<% @header = "Editing category ##{@category.id}" %>

<%= form_for(@category, url: admin_category_path(@category), html: {method: :put, class: "form-horizontal"}) do |f| %>
  <%= render 'form', f: f %>
<% end %>

<%= admin_link_to_show @category%>
<%= admin_link_to_siblings_of @category %>
